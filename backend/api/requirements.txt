# CleanCity — FastAPI backend dependencies
# Install:  pip install -r requirements.txt

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.0

# Async ORM + database drivers
sqlalchemy==2.0.36
aiosqlite==0.20.0          # SQLite async driver (dev)
asyncpg==0.30.0            # PostgreSQL async driver (production)

# Data validation
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client (engine + realtime calls)
aiohttp==3.11.2

# File I/O
aiofiles==24.1.0
python-multipart==0.0.12   # form/file upload support

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2              # async test client
